{
  "name": "study-vault",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev --host 0.0.0.0 --port 80 --allowed-hosts=study.tmadhu.me,localhost",
    "start": "astro dev --host 0.0.0.0 --port 80 --allowed-hosts=study.tmadhu.me,localhost",
    "build": "astro build",
    "preview": "astro preview --host 0.0.0.0 --port 80 --allowed-hosts=study.tmadhu.me,localhost"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.32.0",
    "astro": "^5.0.0",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
